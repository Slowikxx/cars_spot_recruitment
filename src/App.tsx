import { HeroSection, Navigation } from './components';

const App = () => {
	return (
		<main className="sm:px-24 px-6 h-full w-full">
			<Navigation />
			<HeroSection />
		</main>
	);
};

export default App;
